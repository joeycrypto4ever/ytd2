export type VideoDetails = {
  title: string;
  content: string;
};

export type faq = {
  question: string;
  answer: string;
};

export type feature = {
  imageUrl: string;
  title: string;
  description: string;
};

export type SingleLink = {
  name: string;
  path: string;
};
